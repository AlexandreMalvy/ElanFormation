{% extends 'base.html.twig' %}

{% block title %}liste des Stagiaire De Session
{% endblock %}

{% block body %}
	<a href="{{path('plannings_index')}}">Retour à liste des Session
	</a>
	{% if editMode %}
		<h1>Modification d'un stagiaire</h1>
	{% else %}
		<h1>Ajout d'un stagiaire</h1>
	{% endif %}
	{{ form_start(formStagiaireDeSession)}}
	{{form_row(formStagiaireDeSession.nom)}}
	{{form_row(formStagiaireDeSession.prenom)}}
	{{form_row(formStagiaireDeSession.email)}}
	{{form_row(formStagiaireDeSession.numeroPoleEmploi)}}
	{{form_row(formStagiaireDeSession.adresse)}}
	{{form_row(formStagiaireDeSession.cp)}}
	{{form_row(formStagiaireDeSession.ville)}}
    {{form_row(formStagiaireDeSession.telephone)}}
	<button type="submit" class="btn">valider</button>
	{{ form_end(formStagiaireDeSession)}}
{% endblock %}